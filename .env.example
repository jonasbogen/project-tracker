# Local development only. Copy to .env at the repo root and fill in a local Postgres connection string.
# In Minato, DATABASE_URL is injected automatically by the managed database (minato_enable_database).
DATABASE_URL=postgres://localhost:5432/project_tracker
PORT=8080
