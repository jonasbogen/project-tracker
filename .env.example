# Local development only. Copy to .env and fill in a local Postgres connection string.
# In Minato, DATABASE_URL and PG* variables are injected automatically by the managed database.
DATABASE_URL=postgres://localhost:5432/project_tracker
PORT=8080
